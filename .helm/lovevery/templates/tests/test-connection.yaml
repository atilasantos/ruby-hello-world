apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reby-hello.fullname" . }}-test-connection"
  labels:
    {{- include "reby-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reby-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
